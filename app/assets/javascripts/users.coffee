# Place all the behaviors and hooks related to the matching controller here.
# All this logic will automatically be available in application.js.
# You can use CoffeeScript in this file: http://coffeescript.org/

#jQuery -> 
    #locations = $('#locations').html()
 #   $('#locations').change ->
        
        # location =  $('#locations option:selected').text()
        # $('#category_textbox').val(location)
        
        # $.post('users',
        #   {location_post: JSON.stringify(location)})
        # $('#area').html('<%= j render(:partial => \'by_area\' %>')   
        # html = "<%= escape_javascript(render partial: \'by_area\') %>"
        # $('#area').append(html)
        #$("#items_grid").html("<%= escape_javascript(render partial: 'items_list', locals: { items: @selected } ) %>"); 
  #      console.log(location)
        
#handleViewedItem: (data) ->
#    // Do something now that the AJAX call is complete.

# jQuery ->
#     $('#locations').change ->
         
#       $('div#area').html('<%= j render(:partial => \'by_area\') %>') 
      #$('#insert-here').append('<%= j render(:partial => \'participant_data\') %>')
         
     
# #GetViewedItem: (foo) ->
#     $.ajax '/Foo/Bar/',
#         type: 'GET',
#         data: { id: $(foo).data('fooId') }
#         success: (data) ->
#             handleViewedItem data